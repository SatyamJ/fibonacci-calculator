{
  "name": "worker",
  "version": "1.0.0",
  "description": "node worker to calculate fibonacci and store back in redis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon"
  },
  "author": "Satyam Jaiswal",
  "license": "ISC",
  "dependencies": {
    "redis": "^2.8.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
